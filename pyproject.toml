[project]
name = "luneburg-lens-generator"
version = "0.3.0"
description = "Luneburg lens STL generator (PyMesh)"
readme = "README.md"
requires-python = ">=3.7,<3.8"
dependencies = [
  "numpy==1.21.6",
]

[project.optional-dependencies]
# 仅用于本地 lint/类型检查等；运行仍以 Docker 镜像为准。
dev = []
